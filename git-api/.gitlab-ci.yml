before_script:
  - echo "starting..."
  - cd c:/destination/mygitlab
  - dir
  - git config --global --add safe.directory c:/destination/mygitlab
  - git pull
  # - if not exist testing\ mkdir testing

stages:
  - build

build:
  stage: build
  script: echo "Hello World!"
  tags:
    - training-innotex-day3

